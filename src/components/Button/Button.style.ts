import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    margin: 4,
    padding: 4,
    borderRadius: 4,
    alignSelf: "flex-start",
    maxWidth: 128,
  },
});
